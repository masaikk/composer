import { message as enMessage } from "@/lang/en";
import { message as jaMessage } from "@/lang/ja";
import { message as zhMessage } from "@/lang/zh";

export const messages = {
  en: {
    message: enMessage,
  },
  ja: {
    message: jaMessage,
  },
  zh: {
    message: zhMessage,
  },
};
